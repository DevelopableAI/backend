Generate Python test code for validating entity creation via a REST API (missing required field tests only).

The placeholder comment describes the entity, its canonical create path, required fields, and available state variables.

Rules:
- CRITICAL: You MUST only generate tests for fields that are EXPLICITLY listed in the required fields comment block. Do NOT add tests for any other field, even if domain knowledge suggests it should be required. Fields with database defaults (e.g. status, total) are intentionally optional in the API.
- If the required fields list is EMPTY, output exactly one line: `# (no required fields to validate)`
  Do NOT output any other code, guards, or tests. This is a hard rule — an empty list means zero tests.
- Use realistic field values matching the field name's implied format: "email" fields → "test@example.com"; "url"/"Url" fields → "https://example.com"; "title" fields → "Test Title"; "content"/"body" fields → "Test content here"; "name" fields → "Test Name"; other strings → "Test Value"
- CRITICAL: ALL string values MUST fit on a single line. Never write a string literal that spans multiple lines. Keep values short (under 80 characters).
- Otherwise, for EACH required field listed: generate one test that omits only that field and expects HTTP 400
  - Use `ctx.assert_status(resp, 400, "POST <entity> missing <field> → 400")`
  - Include all OTHER required fields in the body (use values matching the field name format as described above)
  - Wrap each test in an `if token1:` guard with a fully-formed body inside — never emit a bare `if token1:` with no body
- After the missing-field tests, generate ONE ownership spoofing test if the owner FK is listed in the placeholder:
  - Include the owner FK field with user2_id in the body while using token1
  - Use `ctx.assert_status` and store the created id in `ctx.state["spoofed_<entity_lower>_id"]`
- Do NOT generate a PUT/update test — that is handled by the template separately
- Include any secondary FK fields listed in the placeholder's "Secondary FK fields" block in EVERY request body — these are NOT server-injected; the test must supply them. Do NOT include the owner FK or primary parent FK (those are server-injected)
- Variable names: token1, token2, <entity_lower>1_id, <entity_lower>2_id, etc. are already declared — use them exactly as listed in the placeholder. Do NOT invent new variable names like nested_*_id.
- IMPORTANT: Use the `body=` keyword argument (NOT `json=`) for request bodies in ctx.req()
- CRITICAL INDENTATION: Every outermost statement — including `if token1:`, `resp =`, comments, and `ctx.state[...]` — MUST start at column 0 (zero leading spaces). Only code nested INSIDE an `if` or `for` block uses 4-space relative indent.
  WRONG (do NOT do this):
      if token1:                 ← 4 leading spaces on an outermost statement
          resp = ctx.req(...)
  RIGHT:
  if token1:                     ← 0 leading spaces
      resp = ctx.req(...)        ← 4 spaces, nested inside the if

Output only the Python code. No function definitions, no imports.
